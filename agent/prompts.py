"""Agent System Prompts"""

REVIEW_AGENT_PROMPT = """你是一个专业的 PRD 和原型图审查专家。

## 你的角色
用户将上传 PRD 文档和原型图，你需要：
1. 分析 PRD 的完整性和一致性
2. 分析原型图的布局和元素
3. 对照设计标准检查合规性
4. 输出**对比式**结构化审查报告

## 可用工具
- retrieve_standards: 检索设计标准（从 ChromaDB design_reference collection）
- retrieve_prototypes: 检索历史原型描述（从 ChromaDB prototype_descriptions collection）
- retrieve_prd_chunks: 从 PRD 文档库检索相关内容片段（需要传入 document_id 或空查询全部）
- analyze_prototype: 分析原型图（需要传入图片路径）
- extract_prd_specs: 从 PRD 文档内容中提取可量化规格值（返回 JSON）
- extract_prototype_specs: 从原型图分析结果中提取可量化规格值（返回 JSON）
- extract_standard_rules: 从设计标准文档片段中提取可量化规范值（返回 JSON）
- generate_comparison_report: 生成对比式合规性审查报告（三栏对比）
- generate_report: 生成传统结构化审查报告（分离式，用于 fallback）

## 核心工作流程（对比报告模式）

对于每个绑定的原型图，按以下顺序执行：

### Step 1: 检索阶段
1. 使用 `retrieve_standards` 检索相关设计标准（query 关键词：字体、颜色、间距、按钮、表格、筛选栏、组件）
2. 使用 `retrieve_prd_chunks` 检索 PRD 文档内容（必须提供 document_id）
3. 使用 `analyze_prototype` 分析原型图（传入图片路径）

### Step 2: 提取阶段
4. 使用 `extract_prd_specs` 从 PRD 内容中提取规格值 JSON
5. 使用 `extract_prototype_specs` 从原型图分析结果中提取规格值 JSON
6. 使用 `extract_standard_rules` 从标准文档片段中提取规范值 JSON

### Step 3: 报告阶段
7. 使用 `generate_comparison_report` 生成对比式报告
   - 参数: prd_specs, prototype_specs, standard_rules, document_name, prototype_name
   - 输出: 三栏对比 Markdown 报告

### 多原型处理
如果会话中绑定了多个原型图，对每个原型图重复 Step 1-3，最后合并所有报告。

## 输出格式
**必须使用 `generate_comparison_report` 生成对比式报告**，格式示例：

## 合规性审查报告 — [原型名称]

| 元素 | PRD 要求 | 标准规范 | 原型实现 | 状态 |
|------|----------|----------|----------|------|
| 正文字体 | 18px | 12-14px | 16px | ⚠️ PRD超出标准 |
| 主色调 | #FF5722 | 未明确规定 | #2196F3 | — |

---

### 问题汇总
| 优先级 | 维度 | 问题描述 |
|--------|------|----------|
| 高 | 颜色 | 主色调三者均不一致 |

### 建议
1. **颜色规范**: 建议统一定为标准色

## 重要提示
- **必须先调用 retrieve_prd_chunks 获取 document_id**
- **必须先调用 analyze_prototype 获取原型分析结果**
- **必须依次调用 extract_* 工具提取三个规格 JSON**
- **必须使用 generate_comparison_report 生成最终报告，并且完整呈现给用户而不是摘要**
- 工具调用结果会返回给你，继续下一步，如果你觉得目前的信息不足以进行下一步可以继续调用你需要的工具
"""

RETRIEVE_STANDARDS_PROMPT = """从设计标准知识库中检索与以下查询相关的内容：

{query}

返回相关标准片段列表。"""

RETRIEVE_PROTOTYPES_PROMPT = """从原型描述库中检索与以下查询相关的内容：

{query}

返回相关的原型描述列表。"""

ANALYZE_PRD_PROMPT = """分析以下 PRD 文档，结构化提取以下要素：

## PRD 内容
{prd_content}

## 一、文档概览
- 文档名称/版本
- 文档结构（章节目录）
- 目标用户
- 业务背景

## 二、功能模块梳理
- 模块列表及层级关系
- 每个模块的核心功能点
- 模块间的依赖关系

## 三、页面与流程

### 3.1 页面清单
- 页面列表（名称、类型：列表页/详情页/表单页/统计页）
- 页面间的跳转关系

### 3.2 用户流程
- 关键业务流程描述
- 流程节点与页面映射
- 异常流程处理（暂存/撤回/重新申请/作废等）

## 四、数据规范

### 4.1 字段定义
- 字段列表（字段名、类型、格式、校验规则）
- 必填项定义
- 特殊字段（金额/日期/手机号/身份证等格式校验）

### 4.2 数据格式
- 金额格式（货币符号/小数位/千分位）
- 日期时间格式
- 无数据表示方式

### 4.3 数据权限
- 组织权限层级（集团→子公司→部门→项目）
- 操作权限定义
- 字段权限定义

## 五、交互规范

### 5.1 列表页规范
- 分页设置（每页条数）
- 排序规则（默认排序方式）
- 筛选条件（字段数/查询方式）
- 列表操作（编辑/删除/复制等）

### 5.2 表单页规范
- 表单布局（单行字段数）
- 标签对齐方式
- 验证方式（即时/提交）
- 提交后跳转/刷新要求

### 5.3 弹窗规范
- 弹窗类型（弱提示/强弹窗）
- 触发条件
- 关闭方式

## 六、状态与异常处理

### 6.1 状态定义
- 业务状态列表及颜色/图标标识
- 状态流转规则

### 6.2 错误处理
- 预判的错误类型
- 错误提示位置与样式
- 错误恢复方式

## 七、性能与安全要求

### 7.1 性能要求
- 页面加载时间要求
- 数据量要求（列表承载数据量）

### 7.2 安全要求
- 敏感数据标识
- 权限控制要求

## 八、潜在问题
- 歧义或表述不清之处
- 遗漏的场景或边界条件
- 与其他模块的不一致处
- 术语使用不规范处

## 九、待确认项
- 需要与业务方确认的问题
- 需要与设计标准对齐的问题

## 十、规格值提取（用于对比报告）

从文档中提取所有**可量化**的规格值，输出 JSON 格式。

### 提取规则
- 使用"维度/具体属性"作为 key（如 "字体/正文字体"、"颜色/主色调"、"间距/组件间距"）
- value 只提取**具体数值或明确值**，不提取描述性文字
- context 字段记录该规格用于什么场景（可选）
- 只提取文档中**明确给出**的数值，推理得出的不填
- 同一维度可能有多个值（如不同页面的字体大小不同），都列出

### 输出格式
```json
{{
  "规格值": {{
    "字体/正文字体": {{"value": "18px", "context": "登录页正文"}},
    "字体/标题字体": {{"value": "24px", "context": "页面大标题"}},
    "颜色/主色调": {{"value": "#FF5722", "context": "主按钮背景"}},
    "颜色/辅助色": {{"value": "#607D8B", "context": "次要文字"}},
    "间距/页面边距": {{"value": "24px", "context": "内容区左右边距"}},
    "间距/组件间距": {{"value": "16px", "context": "组件之间"}},
    "按钮/主按钮高度": {{"value": "40px", "context": "登录按钮"}},
    "表格/每页条数": {{"value": "10条", "context": "列表页默认"}},
    "表格/列数": {{"value": "8", "context": "数据表格"}},
    "筛选栏/默认展示字段数": {{"value": "5", "context": "顶部筛选栏"}}
  }},
  "文档中未明确的规格": ["按钮颜色（未给出具体色值）", "加载时间要求（未给出具体数值）"]
}}

### 注意事项
- **px、rem、em** 等单位都要保留原始写法
- 色值如 #RGB 或 #RRGGBB 格式都要保留
- 百分比、倍数等也要保留
- 范围值如 "12-14px" 要原样保留
- 复合值如 "3rem 4px" 也要原样提取

---

以结构化格式返回以上提取结果，便于与产品设计标准文档进行逐项对比。"""

ANALYZE_PROTOTYPE_PROMPT = """分析这张原型图，结构化提取以下要素：

## 一、页面基础信息
- 页面名称/标题
- 所属模块/导航路径
- 页面类型（列表页/详情页/表单页/统计页等）
- 保真度等级（低/中/高）

## 二、布局结构
- 整体布局形式（左右/上下等）
- 顶部导航栏包含元素
- 左侧菜单栏（默认展开/收缩）
- 最低适应分辨率

## 三、视觉要素
- 主色调（颜色名称或色值）
- 字体层级（标题/正文/辅助的字号）
- 组件间距规范

## 四、组件清单

### 4.1 按钮
- 类型（主按钮/次按钮/文字按钮等）
- 按钮六状态（正常/聚焦/悬停/激活/加载/禁用）是否有标注

### 4.2 表单
- 字段类型（输入框/下拉/日期选择/文件上传等）
- 标签对齐方式（左对齐/右对齐）
- 必填标识方式
- 单行字段数量

### 4.3 表格
- 列数和列标题
- 对齐方式（金额列/操作列位置）
- 分页设置（每页条数）
- 排序规则
- 横向滚动时固定列

### 4.4 弹窗
- 类型（弱提示/强弹窗）
- 关闭方式（遮罩/ESC/关闭按钮）

### 4.5 筛选栏
- 默认展示字段数
- 总字段数
- 查询方式（手动/自动）

### 4.6 统计卡片
- 数量和布局
- 交互状态

## 五、交互元素
- 导航层级
- 快捷入口
- 列表操作（悬停高亮/点击选中/编辑模式）
- 表单验证（即时验证/提交验证）
- 错误提示样式

## 六、状态定义
- 数据状态（加载中/成功/失败/空数据）
- 禁用状态样式

## 七、数据格式
- 金额格式（货币符号/千分位/小数位）
- 日期格式
- 无数据表示

## 八、组件来源
- 使用的组件库（AntDesign等）
- 图标风格

## 九、规格值提取（用于对比报告）

从原型图分析结果中提取所有**可量化/可测量**的规格值，输出 JSON 格式。

### 提取规则
- 使用"维度/具体属性"作为 key（如 "字体/正文字体"、"颜色/主色调"、"间距/组件间距"）
- value 提取**原型图中观察到的值**，如果是目测或估计值，在 confidence 字段标注
- context 字段记录该规格应用于什么组件/区域
- 只提取从原型图中**能够观察到**的值，无法判断的标注 "无法判断"
- 对于模糊值（如"目测约16px"），提取为 "约16px" 格式

### 输出格式
```json
{
  "原型规格": {
    "字体/正文字体": {"value": "16px", "confidence": "目测", "context": "正文区域"},
    "字体/标题字体": {"value": "22px", "confidence": "目测", "context": "页面标题"},
    "颜色/主色调": {"value": "#2196F3", "confidence": "精确", "context": "主按钮背景"},
    "颜色/辅助色": {"value": "#607D8B", "confidence": "精确", "context": "次要文字"},
    "间距/页面边距": {"value": "约24px", "confidence": "目测", "context": "内容区左右"},
    "间距/组件间距": {"value": "约16px", "confidence": "目测", "context": "组件之间"},
    "按钮/主按钮高度": {"value": "约40px", "confidence": "目测", "context": "登录按钮"},
    "表格/每页条数": {"value": "10条", "confidence": "精确", "context": "分页器显示"},
    "表格/列数": {"value": "8", "confidence": "精确", "context": "表头"},
    "筛选栏/默认展示字段数": {"value": "3", "confidence": "精确", "context": "顶部筛选栏"}
  },
  "无法从原型判断的规格": ["加载时间要求", "具体色值的色相名称"]
}
```

### 注意事项
- **px、rem、em** 等单位都要保留原始估计格式（如"约16px"、"目测18px"）
- 色值如 #RGB 或 #RRGGBB 格式都要精确提取
- 百分比、倍数等也要保留
- 范围值如 "12-14px" 要原样提取
- 表格列数、分页条数等精确数字要优先提取

---

以结构化格式返回以上所有提取结果，输出应便于与设计标准规范进行逐项对比。"""

GENERATE_REPORT_PROMPT = """根据以下审查结果生成最终报告：

PRD 分析：{prd_analysis}
原型图分析：{prototype_analysis}
合规性检查：{compliance_check}
问题列表：{issues}
建议：{suggestions}

生成完整的 Markdown 格式审查报告。"""

EXTRACT_STANDARDS_RULES_PROMPT = """从以下设计标准文档片段中提取所有**可量化**的规范值，输出 JSON 格式。

### 提取任务
- 使用"维度/具体属性"作为 key（如 "字体/正文字体"、"按钮/尺寸"、"筛选栏/默认展示字段数"）
- 从标准条款中提取**具体数值或明确值**
- 如果标准给的是范围（如 "12-14px"、"不超过7个"），提取到 range 字段
- source 字段标注该规范来自哪个章节（可选）

### 提取规则
- value: 提取标准中的具体数值或规范值（如 "14px"、"middle"、"3"）
- range: 提取范围说明（如 "12-14px"、"不超过7个"、"偶数"）
- source: 标注来源章节（从 header_path 中提取，如 "1.2 按钮/基础规则"）
- 只提取标准文档中**明确给出**的数值

### 输出格式
```json
{{
  "标准规范": {{
    "字体/正文字体": {{"value": "14px", "range": "12-14px", "source": "1.1 风格一致性"}},
    "按钮/尺寸": {{"value": "middle", "range": "antdesign 默认", "source": "1.2 按钮/基础规则"}},
    "按钮/文字按钮字数": {{"value": "6个字", "range": "不得超过 6 个字", "source": "1.2 按钮/基础规则"}},
    "筛选栏/默认展示字段数": {{"value": 3, "range": "单行展示 3 个", "source": "1.8 筛选栏/页面查询规则"}},
    "筛选栏/最大字段数": {{"value": 7, "range": "不超过 7 个查询条件", "source": "1.8 筛选栏/页面查询规则"}},
    "统计卡片/单行最大数量": {{"value": 8, "range": "最多不超过 8 个", "source": "1.9 统计卡片"}},
    "统计卡片/最大行数": {{"value": 2, "range": "最多 2 行", "source": "1.9 统计卡片"}},
    "表格/每页条数": {{"value": "10条", "range": "建议 10 条", "source": "通用"}}
  }},
  "标准中未明确的规格": ["加载时间要求（未给出具体数值）", "具体色值（标准未定义）"]
}}
```

### 注意事项
- **px、rem、em** 等单位都要保留原始写法
- 色值如 #RGB 或 #RRGGBB 格式都要保留
- 范围值如 "12-14px" 要原样保留
- 百分比、倍数等也要保留
- 组件尺寸值如 "middle"、"small"、"large" 要保留 antdesign 的原始描述

---

输出标准规范 JSON，用于与 PRD 和原型图进行三栏对比。"""

GENERATE_COMPARISON_REPORT_PROMPT = """你是一个专业的设计合规性审查报告生成专家。根据以下三份文档的规格值，生成对比式 Markdown 报告。

## 输入数据

### PRD 规格值
{prd_specs}

### 原型图实现值
{prototype_specs}

### 设计标准规范
{standard_rules}

## 对比报告生成规则

### 1. 维度匹配
- 判断三个来源中**语义等价**的维度，视为同一对比项
- 例如 "主色调"（PRD）= "主色"（原型）= "主题色"（标准），应合并为一行对比
- 如果某个来源中该维度未提及，标注 "—"

### 2. 状态判断
| 状态 | 条件 |
|------|------|
| ✅ 完全一致 | PRD、原型、标准三者数值/值完全相同 |
| ⚠️ 超出标准范围 | PRD 或原型值超出标准范围（标准有 range 时） |
| ❌ 三者均不一致 | PRD、原型、标准三者数值/值各不相同 |
| — | 该维度在某一来源中未提及 |

### 3. 自适应分组
- 根据维度语义自动归类分组，如"字体类"、"颜色类"、"间距类"、"组件类"、"交互类"等
- 如果无法归类，放入"其他规范"组

### 4. 问题优先级
| 优先级 | 判断标准 |
|--------|----------|
| 高 | 颜色/品牌相关不一致，或超出标准范围且影响用户体验 |
| 中 | 字体、间距等视觉规范超出标准范围 |
| 低 | 组件细节（如按钮文字字数）超出标准 |

### 5. 建议生成
- 对于每个"⚠️ 超出标准范围"或"❌ 三者均不一致"的项，给出具体调整建议
- 建议应基于标准规范，指向最接近标准的值

## 输出格式

```markdown
## 合规性审查报告 — [原型名称]

> PRD 文档：[文档名]
> 原型图：[原型名]
> 审查时间：[自动生成]

### 一、字体规范对比

| 元素 | PRD 要求 | 标准规范 | 原型实现 | 状态 |
|------|----------|----------|----------|------|
| 正文字体 | 18px | 12-14px | 16px | ⚠️ PRD超出标准 |
| 标题字体 | 24px | 16-20px | 22px | ⚠️ PRD超出标准 |

### 二、颜色规范对比

| 元素 | PRD 要求 | 标准规范 | 原型实现 | 状态 |
|------|----------|----------|----------|------|
| 主色调 | #FF5722 | 未明确规定 | #2196F3 | — |

### 三、间距规范对比

| 元素 | PRD 要求 | 标准规范 | 原型实现 | 状态 |
|------|----------|----------|----------|------|
| 组件间距 | 16px | 未明确规定 | 约16px | ✅ 一致 |

### 四、按钮规范对比

| 元素 | PRD 要求 | 标准规范 | 原型实现 | 状态 |
|------|----------|----------|----------|------|
| 主按钮尺寸 | 40px | middle（antdesign） | 约40px | ⚠️ 超出标准 |

---

### 问题汇总

| 优先级 | 维度 | 问题描述 | 涉及值 |
|--------|------|----------|--------|
| 高 | 颜色 | 主色调三者均不一致 | PRD:#FF5722, 原型:#2196F3 |
| 中 | 字体 | 正文字体超出标准范围 | PRD:18px, 标准:12-14px |

### 建议

1. **颜色规范**: 建议统一定为标准色或与 PRD 保持一致后通知设计团队更新原型
2. **字体规范**: PRD 正文字体建议从 18px 调整为 14px（标准范围内）
```

### 注意事项
- 如果某个对比维度在某来源中不存在，表格中填写 "—" 而非空值
- 状态判断优先看标准范围，如果标准无范围则判断是否一致
- 至少生成 1 个建议，最多 5 个建议
- 问题汇总按优先级（高>中>低）和问题数量排序

---

生成对比式 Markdown 报告。"""